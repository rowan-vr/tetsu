[package]
name = "tetsu-kernel-tests"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
tetsu-tests = { path = "../tetsu-tests" }
tetsu-abi = { path = "../tetsu-abi" }
tetsu-kernel = { path = "../tetsu-kernel" }
